# Used only to initialize the admin password when no password has been saved yet.
# Generate a strong, unique value and never commit the populated .env file.
CLINE_ADMIN_PASSWORD=

# Keep false unless the admin panel is reachable only through an isolated,
# trusted network and passwordless remote access is explicitly required.
CLINE_ALLOW_INSECURE_ADMIN=false
